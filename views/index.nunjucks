{% extends "base.nunjucks" %}

{% block body %}
  <div class="mui-row">
    <div class="mui-col-md-12">
      <div class="mui--pull-left">
        <a href="/schedule" class="mui-btn mui-btn--primary mui-btn--raised">Schedule a post</a>
      </div>
      <div class="mui--pull-right">
        <a href="/authorize" class="mui-btn mui-btn--raised">Manage authorizations</a>
        <a href="/logout" class="mui-btn mui-btn--raised">Logout</a>
      </div>
    </div>

    {% for message in messages %}
      <div class="mui-col-md-12">
        <div class="mui-panel message--body">
          {{ message }}
        </div>
      </div>
    {% endfor %}

    <div class="mui-col-md-12">
      <div class="mui-panel posts--body">
        <div class="mui--text-center">
          <h1>Scheduled posts</h1>
        </div>
        <p>There are no scheduled posts yet</p>
      </div>
    </div>
  </div>
{% endblock %}
